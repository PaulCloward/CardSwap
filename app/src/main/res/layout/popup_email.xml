<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/popup_padding"
    >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <TextView
            android:id="@+id/tv_popup_email_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/popup_email_title"
            android:textSize="@dimen/popup_title_text_size"
            android:layout_weight=".9"
            android:textColor="@color/colorPopupTitleText"
            />
        <ImageView
            android:id="@+id/iv_popup_email_checkmark"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_check"
            android:layout_weight=".1"
            />
    </LinearLayout>

    <EditText
        android:id="@+id/et_popup_email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textEmailAddress"
        android:hint="@string/popup_email_hint"
        android:layout_marginBottom="@dimen/popup_margin_bottom"
        android:backgroundTint="@color/colorEditTextUnderline"></EditText>

</LinearLayout>